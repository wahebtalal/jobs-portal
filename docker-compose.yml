version: '3.9'
services:
  jobs-portal:
    build: .
    env_file:
      - .env
    environment:
      - PORT=${PORT:-3000}
    ports:
      - "${HOST_PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./prisma:/app/prisma
    restart: unless-stopped
